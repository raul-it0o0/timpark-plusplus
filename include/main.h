#pragma once
#include <iostream>
#include <array>
#include <vector>
#include <stdlib.h>

namespace stlUtils {
    using std::cin;
    using std::cout;
    using std::endl;
    using std::array;
    using std::vector;
    using std::to_array;
    using std::find;
    using std::string;
    using std::ifstream;
    using std::ofstream;
    using std::getline;
    using std::flush;
    using std::fstream;
    using std::ifstream;
}
